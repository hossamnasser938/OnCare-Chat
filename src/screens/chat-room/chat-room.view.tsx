import React from 'react';
import {Text} from 'react-native';

import {IChatRoomViewProps} from './chat-room.types';

export const ChatRoomView = (props: IChatRoomViewProps) => {
  return <Text>ChatRoomView</Text>;
};
